version: '3.8'

services:
  twinclaw:
    build: .
    container_name: twinclaw-agent
    restart: always
    ports:
      - "18789:18789"
    volumes:
      - ./data:/home/node/.twinclaw/workspace
    environment:
      - TWINCLAW_SAFE_CWD=/home/node/.twinclaw/workspace
    env_file:
      - .env
